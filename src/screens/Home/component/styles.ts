import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    content: {
        fontSize: 24,
        padding: 16,
    },
    container: {
        borderWidth: 1,
        borderRadius: 20,
        marginBottom: 32
    }
})

export default Styles